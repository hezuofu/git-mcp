{
  "name": "git-mcp-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @git-mcp/core build",
    "test": "pnpm -r test",
    "dev": "pnpm build:core && node packages/core/build/index.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
